<message from="{{sender}}">
{{payload}}
</message>

If the payload contains a `photo_path`, use the Read tool to view the image at that path BEFORE responding — you can see images.

Please respond directly using `telegram send "<chat_id>" "..."` where chat_id comes from the payload.
