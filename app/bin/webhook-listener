#!/bin/bash
# Webhook SSE listener daemon
# Connects to the smee.io-compatible relay and fires webhook triggers on events.
#
# supervisord config:
#   [program:webhook-listener]
#   command=/talos/app/bin/webhook-listener
#   autostart=true
#   autorestart=true
#   stdout_logfile=/talos/logs/webhook-sse.log
#   stderr_logfile=/talos/logs/webhook-sse-error.log
exec bun /talos/app/triggers/webhook-sse-listener.ts "$@"
